
ssh-keygen -t ed25519 -C "your_email@example.com" will Generate a ssh key.

eval "$(ssh-agent -s)" will start the ssh agent in the background.

ssh-add ~/.ssh/id_ed25519 add the ssh key to ssh agent.

xclip -selection clipboard < ~/.ssh/id_ed25519.pub Copy the ssh key.
